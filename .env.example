# Copy to .env (repo root) on the server - docker-compose reads this
# automatically. Only used to set the Postgres password; must match the
# password inside backend/.env.production's DATABASE_URL exactly.

POSTGRES_PASSWORD=REPLACE_WITH_A_LONG_RANDOM_PASSWORD
